name: Build

on:
  push:
    branches:
  pull_request:

jobs:
  build:
    name: Build (Linux)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable

      - run: cargo check
      - run: cargo build
      - run: cargo test
