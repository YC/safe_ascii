[package]
name = "safe_ascii"
version = "1.0.1"
edition = "2018"

[dependencies]
clap = { version = "3.1.6", default-features = false, features=["std"] }

[profile.release]
opt-level = 3
debug = false
lto = true
panic = 'unwind'
codegen-units = 1
